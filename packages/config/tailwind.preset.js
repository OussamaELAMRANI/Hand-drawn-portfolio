/** @type {import('tailwindcss').Config} */
export default {
    theme: {
        extend: {
            // your design tokens — e.g. the hand-drawn sketchbook palette
            colors: {
                // ink: '#1a1a2e',
                // paper: '#faf7f0',
            },
            fontFamily: {
                // sketch: ['"Caveat"', 'cursive'],
            },
        },
    },
    plugins: [],
}